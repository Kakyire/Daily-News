# Uncomment the next line to define a global platform for your project
 platform :ios, '14.0'

target 'Daily News' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Daily News
  pod 'SDWebImage'

plugin 'cocoapods-keys', {
  :project => "Daily News",
  :target => "Daily News",
  :keys => [
    "NEWS_API_KEY"
  ]
}

end
